@model IEnumerable<StudentApp.Models.OdemePlanlari>

@{
    ViewData["Title"] = "Ödeme Planları";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2><i class="fas fa-calendar-alt me-2"></i>Ödeme Planları</h2>
    <div>
        <div class="form-check form-switch d-inline-block me-3">
            <input class="form-check-input" type="checkbox" id="showPasifSwitch"
                   @(ViewBag.ShowPasif == true ? "checked" : "")
                   onchange="togglePasif(this.checked)">
            <label class="form-check-label" for="showPasifSwitch">
                <i class="fas fa-eye-slash me-1"></i>Pasif Kayıtları Göster
            </label>
        </div>
        <a asp-action="Create" class="btn btn-primary">
            <i class="fas fa-plus me-1"></i>Yeni Ödeme Planı Ekle
        </a>
    </div>
</div>

<script>
    function togglePasif(showPasif) {
        const url = new URL(window.location);
        url.searchParams.set('showPasif', showPasif);
        window.location.href = url.toString();
    }
</script>

@if (Model.Any())
{
    <div class="row mb-3">
        <div class="col-md-6">
            <div class="alert alert-info mb-0">
                <i class="fas fa-info-circle me-1"></i>
                @if (ViewBag.ShowPasif == true)
                {
                    <span>Toplam <strong>@Model.Count()</strong> ödeme planı bulundu (aktif ve pasif kayıtlar dahil).</span>
                }
                else
                {
                    <span>Toplam <strong>@Model.Count()</strong> aktif ödeme planı bulundu.</span>
                }
            </div>
        </div>
        <div class="col-md-6 text-md-end">
            <form asp-action="ExportToExcel" method="post" class="d-inline me-2">
                @Html.AntiForgeryToken()
                <input type="hidden" name="showPasif" value="@(ViewBag.ShowPasif ?? false)" />
                <button type="submit" class="btn btn-success">
                    <i class="fas fa-file-excel me-1"></i>Excel'e Aktar
                </button>
            </form>
            <form asp-action="ExportToPdf" method="post" class="d-inline">
                @Html.AntiForgeryToken()
                <input type="hidden" name="showPasif" value="@(ViewBag.ShowPasif ?? false)" />
                <button type="submit" class="btn btn-danger">
                    <i class="fas fa-file-pdf me-1"></i>PDF'e Aktar
                </button>
            </form>
        </div>
    </div>

    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>@Html.DisplayNameFor(model => model.KursProgrami)</th>
                    <th class="text-center">@Html.DisplayNameFor(model => model.Tutar)</th>
                    <th class="text-center">@Html.DisplayNameFor(model => model.Taksit)</th>
                    <th class="text-center">Taksit Tutarı</th>
                    <th class="text-center">@Html.DisplayNameFor(model => model.Vade)</th>
                    <th class="text-center">@Html.DisplayNameFor(model => model.Aktif)</th>
                    <th class="text-center">İşlemler</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr class="@(!item.Aktif ? "table-secondary" : "")">
                        <td>
                            <i class="fas fa-book me-1"></i>@Html.DisplayFor(modelItem => item.KursProgrami)
                        </td>
                        <td class="text-center">
                            <strong class="text-success">₺ @item.Tutar.ToString("N2")</strong>
                            <br/>
                            <small class="text-muted">Toplam</small>
                        </td>
                        <td class="text-center">
                            <span class="badge bg-primary fs-6">@item.Taksit Taksit</span>
                        </td>
                        <td class="text-center">
                            <span class="badge bg-info fs-6">₺ @item.TaksitTutari.ToString("N2")</span>
                            <br/>
                            <small class="text-muted">Her taksit</small>
                        </td>
                        <td class="text-center">
                            @if (item.Vade.HasValue)
                            {
                                <span class="badge bg-warning text-dark">@item.Vade gün</span>
                            }
                            else
                            {
                                <span class="text-muted">-</span>
                            }
                        </td>
                        <td class="text-center">
                            @if (item.Aktif)
                            {
                                <span class="badge bg-success">
                                    <i class="fas fa-check me-1"></i>Aktif
                                </span>
                            }
                            else
                            {
                                <span class="badge bg-secondary">
                                    <i class="fas fa-times me-1"></i>Pasif
                                </span>
                            }
                        </td>
                        <td class="text-center">
                            <div class="btn-group" role="group">
                                <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-outline-info" title="Detayları Görüntüle">
                                    <i class="fas fa-eye"></i>
                                </a>
                                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-warning" title="Düzenle">
                                    <i class="fas fa-edit"></i>
                                </a>
                                @if (item.Aktif)
                                {
                                    <form asp-action="ToggleAktif" method="post" class="d-inline">
                                        <input type="hidden" name="id" value="@item.Id" />
                                        <input type="hidden" name="aktif" value="false" />
                                        <button type="submit" class="btn btn-sm btn-outline-secondary" title="Pasif Yap" onclick="return confirm('Bu ödeme planını pasif yapmak istediğinizden emin misiniz?');">
                                            <i class="fas fa-ban"></i>
                                        </button>
                                    </form>
                                }
                                else
                                {
                                    <form asp-action="ToggleAktif" method="post" class="d-inline">
                                        <input type="hidden" name="id" value="@item.Id" />
                                        <input type="hidden" name="aktif" value="true" />
                                        <button type="submit" class="btn btn-sm btn-outline-success" title="Aktif Yap" onclick="return confirm('Bu ödeme planını aktif yapmak istediğinizden emin misiniz?');">
                                            <i class="fas fa-check"></i>
                                        </button>
                                    </form>
                                }
                                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-outline-danger" title="Sil">
                                    <i class="fas fa-trash"></i>
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}
else
{
    <div class="text-center py-5">
        <i class="fas fa-calendar-times fa-3x text-muted mb-3"></i>
        <h4 class="text-muted">Ödeme planı bulunamadı</h4>
        <p class="text-muted">İlk ödeme planınızı ekleyerek başlayın.</p>
        <a asp-action="Create" class="btn btn-primary">
            <i class="fas fa-plus me-1"></i>İlk Ödeme Planını Ekle
        </a>
    </div>
}
